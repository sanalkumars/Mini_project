<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <%- include("../partials/adminHeader.ejs") %>
    <h1>Edit Product</h1>
    <form action="/admin/editproducts/<%= product.id %>" method="post" enctype="multipart/form-data">

        <!-- Add the 'enctype' attribute for handling file uploads -->

        <label for="name">Product Name:</label>
        <input type="text" id="name" name="name" value="<%= product.name %>" required><br>

        <label for="category">Category:</label>
        <select id="category" name="category" required>
            <option value="Equipments" <%= product.category === 'Equipments' ? 'selected' : '' %>>Equipments</option>
            <option value="Clothing" <%= product.category === 'Clothing' ? 'selected' : '' %>>Clothing</option>
            <option value="Supplements" <%= product.category === 'Supplements' ? 'selected' : '' %>>Supplements</option>
        </select><br>

        <label for="description">Description:</label>
        <textarea id="description" name="description" rows="6" cols="40" required><%= product.description %></textarea><br>

        <label for="price">status:</label>
        <input type="text" id="status" name="status" value="<%= product.status %>" required><br>
 

        <label for="price">Price:</label>
        <input type="number" id="price" name="price" value="<%= product.price %>" required><br>

        <label for="image">Product Image:</label>
        <input type="file" id="image" name="image" accept="image/*"><br>

        <label for="additionalimages"> Additional images:</label>
        <input type="file" id="additionalimages" name="additionalimages" accept="image/*" multiple required> <br>

        <button type="submit">Update Product</button>
    </form>
</body>
</html>
